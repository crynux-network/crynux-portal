## Debug Requirements

Follow a root-cause-first bugfix protocol. Before changing code, analyze the relevant code path, form a clear hypothesis, and validate the actual failure cause with concrete evidence.

Apply a precise and minimal fix tied directly to the confirmed cause. Do not make speculative changes, broad defensive changes, or fallback-style fixes that mask the underlying problem.
